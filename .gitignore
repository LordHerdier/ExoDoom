# build artifacts
*.o
*.so
*.iso
build/*
docker/build/*

# QEMU serial capture from `make docker-test` / docker-run
serial.log

# Generated by docker/scripts/build.sh step 3b (SCRUM-173) -- .bss length for
# tests/kernel/libos_c_probe/libos_c_probe.c, which has no file bytes to
# extract the way its .text/.data do.
tests/kernel/libos_c_probe/libos_c_probe_layout.h
